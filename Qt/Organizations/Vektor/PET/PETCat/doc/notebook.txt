//--------------------------------------------------------------------------------
добавить информационное сообщение о необходимости ручной перезагрузки после прошивки
пока калибруем только координатные сигналы
//--------------------------------------------------------------------------------
при возврате пакета с ask, надо повторять этот пакет, пока не будет получен ответ, отличный от ask

надо сделать в калибровке:
    проверить на адрес 0
    добавить status_str в update_items
//--------------------------------------------------------------------------------
положить структуру в QVariant
в h-файле после класса
Q_DECLARE_METATYPE(PET_device_remote_t)
в cpp
PET_device_remote_t temp;
device->setData(0, Qt::UserRole, QVariant::fromValue(temp));
достать
QVariant var = root->data(0, Qt::UserRole);
if(var.isValid())
{
    PET_device_remote_t device = var.value<PET_device_remote_t>();
}
//--------------------------------------------------------------------------------
